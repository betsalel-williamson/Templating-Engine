# AST

**Abstract Syntax Tree** — the canonical tree structure produced by a template parser. [Legacy syntax](./legacy-syntax.md) and experimental [modern syntax](./modern-syntax.md) grammars map to the same AST node types so one evaluator can render either surface.
