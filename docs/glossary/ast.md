# AST

**Abstract Syntax Tree** — the canonical tree structure produced by a template parser. Both [legacy syntax](./legacy-syntax.md) and [modern syntax](./modern-syntax.md) grammars map to the same AST node types so one evaluator can render either surface.
