# legacy-template-codegen mini-eval

Required before timed calculator-cli A/B runs.

1. Confirm `pressure-notes.md` RED behaviors (template misuse) still apply.
2. Run `should_trigger=true` prompts with skill — agent should reach for template-language docs and registry→template pattern.
3. Run `should_trigger=false` prompts — plain TS chores without template engine.
4. Log checklist in `eval/last-mini-eval.md` (local; may be gitignored).
